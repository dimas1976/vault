import React from 'react';
import styles from './DetailedCredential';
import CredentialCard from '../../components/CredentialCard/CredentialCard';

export default function DetailedCredential(): JSX.Element {
  return (
    <main>{/* <CredentialCard credential /> */}I am detailed credential</main>
  );
}
